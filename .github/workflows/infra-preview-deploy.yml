name: "infra-preview-deploy"

on:
  push:
    branches:
      - main
    paths:
      - 10_infra/**
      - .github/workflows/**
  workflow_dispatch:


jobs:
  preview:
    name: "Preview"
    runs-on: ubuntsu-latest
    steps:
      - run: |
          echo "Hello World"